{
  "class": "hunter",
  "spec": "beastmastery",
  "weapon_topology": "dual_wield",
  "primary_gem_stat_id": "1",
  "primary_gem_id": 32194,
  "raid_ap_contribution": {
    "enabled": true,
    "note": "Expose Weakness - see adapters/tbc/expose_weakness.py. Same real class-wide debuff mechanic as Survival Hunter, not a Survival-only thing."
  },
  "set_bonus_go_source": "sim/hunter/item_sets.go",
  "apl_source": null,
  "apl_note": "Shares Survival Hunter's exact real settings_template.json base (both specs use the same single wowsims apl file, sim/tbc-new/ui/hunter/dps/apls/default.apl.json, and the identical real TypeSimple rotation JSON confirmed byte-for-byte in the raw preset builds) - only talentsString and classOptions.petType actually differ. No settings_template_2h.json: confirmed via the raw preset data that BM's real 2h_*/dw_* gear variants share this same rotation/apl (no melee-weave-style mechanic exists for BM, unlike Survival's own real weave APL) - the existing 2H Weapon Options side-analysis (non-weave branch) handles the 'would a 2H weapon beat my DW gear' question the same way it already does for Balance Druid.",
  "weapon_topology_note": "BM's own real wowsims presets ship BOTH 2h_*/dw_* gear variants at every phase as equally legitimate builds (no APL/rotation difference between them, confirmed - see apl_note). dual_wield chosen as the canonical topology to match Survival Hunter's own existing convention on this exact same class/candidate pool; 2H is evaluated as a real side-comparison via the existing 2H Weapon Options section, same as Survival. Revisited 2026-08-31 (considered flipping to two_hand-canonical) - confirmed with the user this should stay identical to Survival Hunter's own setup, not diverge; reverted.",
  "hit_target_note": "Built from the real '6% hit' wowsims preset variant (bm/dw_6p.gear.json per phase), per CLAUDE.md's own already-stated default ('keep assuming 6% (moonkin present) per the user's stated raid comp - never silently switch to 9% without being asked') - matches every other profile's own real hit-target assumption.",
  "synthetic_character": true,
  "synthetic_character_note": "No real Beastmastery Hunter alt exists - built via ingest/build_synthetic_character.py from the real wowsims phase3 bm/dw_6p preset, same convention as Test-Elemental-Synthetic/Test-Enhancement-Synthetic."
}
